import { Popover } from "@/components/ui/popover";
import React from "react";

type Props = {};

const Lingue = (props: Props) => {
  return (
    <div>
      <Popover>ei ciao</Popover>
    </div>
  );
};

export default Lingue;
